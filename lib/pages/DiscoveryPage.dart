import 'package:flutter/material.dart';

/// 发现Tab界面
class DiscoveryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(child: new Text("发现Tab界面"));
  }
}
